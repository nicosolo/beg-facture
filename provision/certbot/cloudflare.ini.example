# Cloudflare API credentials for DNS-01 challenge
# Copy this file to cloudflare.ini and update with your credentials

# API Token (recommended - more secure)
dns_cloudflare_api_token = YOUR_CLOUDFLARE_API_TOKEN

# OR use Global API Key (less secure)
# dns_cloudflare_email = your-email@example.com
# dns_cloudflare_api_key = YOUR_GLOBAL_API_KEY

# Note: API Token should have the following permissions:
# - Zone:DNS:Edit for the specific zone
# - Zone:Zone:Read for all zones (to find the zone ID)