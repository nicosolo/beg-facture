{
    "name": "beg-management",
    "private": true,
    "workspaces": [
        "apps/app",
        "apps/api",
        "packages/*"
    ],
    "scripts": {
        "dev": "docker compose up --watch",
        "desktop:dev": "cd apps/desktop && bun run dev",
        "desktop:release": "cd apps/desktop && bun run release",
        "docker:build": "./scripts/build-docker.sh"
    },
    "module": "index.ts",
    "type": "module",
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
